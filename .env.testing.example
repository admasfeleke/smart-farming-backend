APP_NAME="Smart Farming"
APP_ENV=testing
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=warning

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=smart_farming_test
DB_USERNAME=root
DB_PASSWORD=change_me

CACHE_STORE=array
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
MAIL_MAILER=array

# Copy this file to `.env.testing` and update DB credentials.
# Then run:
# php artisan migrate --env=testing --force
# php artisan test
